doctype html
html(xmlns='http://www.w3.org/1999/xhtml', xmlns:svg='http://www.w3.org/2000/svg', xml:lang='en')
  head
    meta(http-equiv='Pragma', content='no-cache')
    meta(http-equiv='content-type', content='text/html; charset=UTF8')
    title Argo
    //#include virtual="basetag.inc"
    // My little icon in the link url 
    link(rel='icon', type='image/png', href='images/ArgoIcon')
    // Activate some overrides to basic configuration. 
    //#set var="PageStyle" value="standard" 
    script(type='text/javascript', charset='utf-8') // These lines pull the Server-side-include (SSI) strings.
gPageName='<!--#echo encoding="none" var="PageStyle"-->';
// This is used to get a query string supplied to the page on the server side, but hidden
// from the javascript because of a URL rewrite_mod redirect, which makes URLs prettier.
gHiddenQueryString=decodeURIComponent('<!--#echo encoding="url" var="QUERY_STRING"-->');
    // Javascript libraries and code. 
    //(#)include virtual="html/libraries.inc"
    //#include virtual="libraries.cgi"
    // Main CSS sheet for basic layout, custom mods. 
    link(rel='stylesheet', href='argo.css', type='text/css', media='all')
    link(rel='stylesheet', href='selected-object-info.css', type='text/css', media='all')
    link(rel='stylesheet', href='local.css', type='text/css', media='all')
  body
    #everything
      #header
        center
          a(href='http://argo-microboone.fnal.gov/server/file_browser.cgi?path=/uboone/data/uboonepro/reco/S2013.06.25', style='background: black; color: white;') Click here for 2013 MC Challenge events!
        center
          // <span style="font-size: 10em;"><img src="images/argo.jpg" alt="Argo - the Microboone Web Event Display" style="height:100px;">Argo</span> 
          |  
          img(src='images/new-argo-logo.png', alt='Argo - the Microboone Web Event Display', style='height:200px;')
        .header-links
          center
            a(href='help') Help and Hints
            a.align-left#Print(href='#') Print
            .align-right
              span.align-right#link-to-this-event Link to this event
              br
              span.align-right#email-this-event Email this event
        table(style='clear: both;', border='0', width='100%')
          tbody
            tr
              td(width='5%') Status:
              td
                #status Script loading...
      // header 
      #expando-target(style='z-index:998;')
      #content
        #content-top
          // INPUT 
          include inc/DataSource 
        // content-top 
        div(style='clear:both;')
        #content-right.dock
          include inc/HitHistogram 
          include inc/EventInfo 
          include inc/Configuration 
          include inc/WireInfo 
          include inc/dEdXTool 
          include inc/MCInfo 
          include inc/HoverInfo 
        // content-right 
        #content-main
          #content-wide.dock
            include inc/TimeHistogram 
            include inc/WireView 
            include inc/reco 
            include inc/ZoomControl 
          // content-wide 
          #content-left.dock
            include inc/MRI 
            include inc/MCDigraph 
          // content-left 
          #content-middle.dock
            include inc/TridView 
            include inc/WireViewBig 
            include inc/ArgoDreamyScan 
          // content-middle 
        // content-main 
      // content 
      // <div style="clear: both;"></div> 
      #footer
        include inc/Debugging 
        include inc/credits 
      // footer 
      // hidden stuff. 
      include inc/MOTD 
      #warning-dialog(title='Warning')
        p#warning-dialog-message
      include inc/SelectionInfoOverlay 
    // Everything 
